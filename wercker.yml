box: wercker/default
build:
  steps:
    - script:
        name: Checkout correct branch
        code: |-
            git checkout site
    - arjen/hugo-build:
        version: 0.12
        theme: redlounge
        flags: --disableSitemap=true
